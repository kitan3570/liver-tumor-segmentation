# 肝脏与肝肿瘤 3D 分割项目报告（模板）

> 本模板供大一医学生填写项目报告使用。请将各章节中 `[占位]` 处替换为你的实际内容。
> 完成后可删除本提示框。

---

## 1. 标题页

- **项目标题**：[占位：如 肝脏与肝肿瘤 3D 分割入门科研项目]
- **作者（姓名 / 学号）**：[占位]
- **日期**：[占位：YYYY-MM-DD]
- **指导老师**：[占位]

---

## 2. 项目背景与动机

[占位：简要说明]

- 肝脏是腹部最大实质脏器，肝肿瘤（含原发性肝癌、转移瘤等）早期检出对预后意义重大；
- CT 是常用影像检查，但人工逐层勾画肝脏与肿瘤耗时且存在观察者间差异；
- 3D 分割能利用体数据的连续性，相比 2D 切片分割更贴合临床三维解剖；
- 本项目以入门科研为目标，帮助初学者理解医学影像 AI 全流程；
- [占位：你自己的学习动机]

---

## 3. 数据集介绍

[占位：请填写以下内容]

- **数据集名称**：Medical Segmentation Decathlon (MSD) Task03_Liver
- **数据来源**：http://medicaldecathlon.com/
- **模态**：腹部 CT（增强 / 平扫，[占位：补充说明]）
- **文件格式**：NIfTI (`*.nii.gz`)
- **标注类别**：
  - `0` = 背景
  - `1` = 肝脏
  - `2` = 肿瘤
- **样本数量**：
  - 训练集：[占位] 例
  - 测试集：[占位] 例
- **影像特点**：体积可变、分辨率可变、HU 值范围较广，需做标准化处理
- [占位：补充你观察到的数据特点，如肿瘤占比小、大小不均等]

---

## 4. 方法

### 4.1 网络结构

- 采用 **3D U-Net**（基于 MONAI 实现）；
- 编码器-解码器 + 跳连接，逐层下采样提取特征，再上采样恢复分辨率；
- [占位：如使用 SE/Res 通道、各层通道数说明]

### 4.2 框架

- **MONAI**：医学影像专用数据变换、网络、滑动窗口推理等；
- **PyTorch**：深度学习后端。

### 4.3 两阶段策略

- **阶段一（二分类）**：背景 vs 肝脏（标签映射 `liver(1)+tumor(2) → liver(1)`）；
- **阶段二（三分类）**：背景 / 肝脏实质 / 肿瘤，在阶段一框定区域内进一步分割；
- [占位：你对该策略优缺点的理解]

### 4.4 数据预处理

- **Spacing 重采样**：统一体素物理 spacing（[占位：如 1.0×1.0×1.0 mm]）；
- **Orientation**：统一方向（如 `RAS`）；
- **CT 窗 (windowing / clipping)**：截断 HU 值并归一化（[占位：如 [-100, 400] → 0-1]）；
- **裁剪 (Crop)**：基于非零区域裁剪，去除多余背景；
- **数据增强**：[占位：如随机翻转、旋转、缩放等]。

### 4.5 损失函数

- **DiceCE Loss**：Dice 损失 + 交叉熵的加权组合，兼顾区域重叠与体素分类；
- [占位：如有类别权重设置请说明]

### 4.6 优化器

- **优化器**：[占位：如 Adam / AdamW / SGD]
- **学习率**：[占位] 与调度策略 [占位：如 ReduceLROnPlateau / CosineAnnealing]

### 4.7 推理

- 使用 **滑动窗口推理 (Sliding Window Inference)** 处理大体积 CT；
- 窗口大小 = `roi_size`，重叠率 [占位：如 0.5]，采用高斯权重融合；
- [占位：是否后处理（最大连通域、去除小噪声等)]

---

## 5. 实验设置

### 5.1 配置

| 项目 | 阶段一 (phase1) | 阶段二 (phase2) |
| --- | --- | --- |
| `roi_size` | [占位] | [占位] |
| `batch_size` | [占位] | [占位] |
| `epochs` | [占位] | [占位] |
| `learning rate` | [占位] | [占位] |
| `device` | [占位：如 cuda] | [占位] |
| `num_workers` | [占位] | [占位] |
| 其它 | [占位] | [占位] |

### 5.2 硬件

- GPU：[占位：如 NVIDIA RTX xxxx, 显存]
- CPU / 内存：[占位]

### 5.3 随机性

- `seed`：[占位：如 42]，固定 numpy / torch / MONAI 随机种子以保证可复现。

---

## 6. 实验结果

### 6.1 阶段一（phase1, binary）指标

| 指标 | Liver |
| --- | --- |
| Dice | [占位] |
| IoU | [占位] |
| Precision | [占位] |
| Recall | [占位] |

### 6.2 阶段二（phase2, ternary）指标

| 指标 | Liver | Tumor |
| --- | --- | --- |
| Dice | [占位] | [占位] |
| IoU | [占位] | [占位] |
| Precision | [占位] | [占位] |
| Recall | [占位] | [占位] |

> 注：若某病例无肿瘤（真值为空），该类别记 `NaN`，不参与均值。

### 6.3 训练曲线

[占位：插入训练/验证 loss 与 Dice 曲线图，文件位于 `outputs/figures/`]

### 6.4 可视化样例

[占位：插入切片叠加图，红色为肝肿瘤、绿色为肝脏等，文件位于 `outputs/figures/`]

### 6.5 STL 三维模型

[占位：导出的肝脏/肿瘤 STL 截图，文件位于 `outputs/stl/`]

---

## 7. 讨论与反思

[占位：请结合你的实验填写，可参考以下提示词]

- **哪些方法有效**：两阶段策略是否帮助了肿瘤分割？DiceCE 是否优于纯 Dice？
- **困难所在**：
  - 肿瘤体积小、类别不平衡严重；
  - 肿瘤与肝脏实质对比度不足；
  - 不同病例 spacing / 体积差异大；
- **小目标难点**：肿瘤占比极低，损失易被肝脏/背景淹没，[占位：你的应对方法]；
- **改进方向**：
  - 引入注意力机制 / 深度监督；
  - 更强数据增强与小目标采样策略；
  - 引入边界损失 (Boundary Loss) 或 focal Dice；
  - 更大 roi_size 或级联网络；
  - [占位：你的其它想法]

---

## 8. 结论

[占位：总结你的工作]

- 完成了基于 3D U-Net 的肝脏 / 肿瘤分割全流程；
- 阶段一 liver Dice 约为 [占位]，阶段二 tumor Dice 约为 [占位]；
- 两阶段策略对小目标肿瘤 [占位：有帮助 / 帮助有限]；
- [占位：你的总体反思与后续计划]

---

## 9. 免责声明

本报告及所对应项目**仅用于科研学习与教学展示**，**不用于临床诊断、治疗或任何医疗决策**。
所用数据为公开 MSD Task03_Liver 数据集，**不含真实患者隐私数据**。
所有指标与结果仅供学习方法对比与教学讨论，不代表任何临床有效性结论。
使用者应遵守数据集及依赖库的许可协议，作者不对任何因使用本报告/项目造成的后果承担责任。